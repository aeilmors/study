#include <iostream>
#include <cstring>
#include <vector>

using namespace::std;

string sa[10];
int ia[10];

int main()
{
	string sa2[10];
	int ia2[10];
	std::vector<int> va(10);

	for (auto it : va) {
		cout << it << endl;
	}

	for (auto it : sa2) {
		cout << it << endl;
	}

	for (auto it : ia) {
		cout << it << endl;
	}

	for (auto it : ia2) {
		cout << it << endl;
	}

	return 0;
}